import React from "react";
import ProjectCallToAction from "./ProjectCallToAction";

const HeroSection7: React.FC = () => {
  return (
    <section className="flex flex-col items-center justify-center py-16 bg-gradient-to-b from-yellow-50 to-white">
      <ProjectCallToAction />
    </section>
  );
};

export default HeroSection7;
